---
description: LW Platform context — read before any cross-project work
alwaysApply: true
---

# LW Platform

Workspace root: `lwp/`. Read `AGENTS.md` at the root first.

- **LW_Portal_2.0** — active Vue 3 admin portal (super-admin only)
- **LW_App** — active Flutter member app (Afrikaans UI)
- **supabase/** — shared backend; all schema changes go in `migrations/`
- **LW_Portal/** — deprecated Svelte portal; do not extend

Plan portal + app + Supabase together. Roadmap: `roadmap.md` at workspace root (single file).
